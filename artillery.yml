config:
  target: 'http://localhost:8181'
  phases:
    - duration : 1
      arrivalRate : 1000
      name: "peak"
  defaults:
    headers:
        Content-Type: 'application/json'
        Authorization: "{{ $processEnvironment.SERVICE_API_KEY }}"
scenarios:
  -
    name: 'Homepage'
    flow:
      - get:
          url: '/'
